import * as React from 'react';
import { LineToResizeElements } from '../constants';
import * as C from './constants';

export const Toolbar: React.FC = ({ }) => {
   return (
      <C.Toolbar>

      </C.Toolbar>
   );
};
